manifestVersion: subsquid.io/v0.1
name: gs-explorer-bitcountry-pioneer
version: 1
description: |-
  Giant Squid Explorer for Bitcountry Pioneer chain
build:

deploy:
  addons:
    postgres:
  processor:
    env:
      CHAIN:
        bitcountry-pioneer
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server", "--dumb-cache", "in-memory", "--dumb-cache-ttl", "1000", "--dumb-cache-size", "100", "--dumb-cache-max-age", "1000" ]

scale:
  addons:
    postgres:
      storage: 35G
      profile: small