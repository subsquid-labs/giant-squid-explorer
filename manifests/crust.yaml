manifestVersion: subsquid.io/v0.1
name: gs-explorer-crust
version: 1
description: |-
  Giant Squid Explorer for Crust chain
build:

deploy:
  addons:
    postgres:
  processor:
    env:
      CHAIN:
        crust
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server", "--dumb-cache", "in-memory", "--dumb-cache-ttl", "1000", "--dumb-cache-size", "100", "--dumb-cache-max-age", "1000" ]

scale:
  addons:
    postgres:
      storage: 300G
      profile: small