manifestVersion: subsquid.io/v0.1
name: gs-explorer-moonsama
version: 1
description: |-
  Giant Squid Explorer for Moonsama chain
build:

deploy:
  addons:
    postgres:
  processor:
    env:
      CHAIN:
        moonsama
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server", "--dumb-cache", "in-memory", "--dumb-cache-ttl", "1000", "--dumb-cache-size", "100", "--dumb-cache-max-age", "1000" ]

scale:
  addons:
    postgres:
      storage: 25G
      profile: small